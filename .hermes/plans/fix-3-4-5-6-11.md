# kkhome 改进计划: 暗色模式 / 路由过渡 / 死代码 / 懒加载

> **任务链:** 5 → 3 → 4 → 6+11

## Task 5: 清除死代码

**文件:**
- `src/App.tsx:136-142` — 空 keydown 事件监听器
- `src/components/Layout.tsx:9-13` — 空 useEffect
- `src/core/PluginSystem.ts:101-106` — 空 initialize() 方法

**改动:**
1. App.tsx: 删除空 `useEffect` + `handleKeydown`
2. Layout.tsx: 删除空 `useEffect`（保留文件以保持导出）
3. PluginSystem.ts: 删除 `initialize()` 和 `isInitialized()` 相关逻辑，或保留但移除 `initialized` 字段

## Task 3: 暗色模式真正实现

**文件修改:**
- `src/index.css` — 为 `:root.dark` 补充真实暗色色值
- `public/config/site.config.json` — 改为 `"theme": "auto"`
- `src/App.tsx` — 在初始化后从 config + localStorage 恢复主题

**暗色调色板思路：** 克莱因蓝在深色背景上更亮，背景 #0f1117，表面 #1a1d27

## Task 4: 路由切换过渡动画

**文件修改:**
- `src/index.css` — 添加路由切换时的渐入动画
- `src/App.tsx` — 给主内容区加 key 触发 remount 动画

## Task 6+11: 插件懒加载

**文件修改:**
- `src/plugins/index.ts` — 用 `React.lazy()` 包装所有插件组件
- `src/App.tsx` — 添加 `Suspense` 包裹 `activePlugin.component` 渲染位置
